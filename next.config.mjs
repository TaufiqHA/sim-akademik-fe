/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed output: "export" and exportPathMap as they're incompatible with app directory
  // For a dashboard app, we want server-side features available
};

export default nextConfig;
